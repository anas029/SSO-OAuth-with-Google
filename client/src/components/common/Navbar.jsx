import Logout from "./Logout"
import Login from "./Login"

const Navbar = () => {
    return (
        <header>
            <Login />
            <Logout />
        </header>
    )
}

export default Navbar